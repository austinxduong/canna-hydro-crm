import React, {useEffect, useState} from 'react'

const useDataSourceSync = () => {
    const [data, setData] = useState()
    const [loading, setLoading] = useState()
    const [error, setError] = useState()

    useEffect(() => {
        try {

        } catch {

        } finally {
            
        }

    }, [])

  return {
    data,
    loading,
    error
  }
}

